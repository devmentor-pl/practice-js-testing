import randomNumber from './app';



describe('randomNumber', () => {
 
    describe('randomNumber', () => {
        it('return number less than 10 when number is less than 10')
        const result = randomNumber(0, 10)
        expect(result).toBe(1)
    })
    it('randomNumber', () => {
        it('return number less than  5 when number is less than 5')
        const result = randomNumber(0, 10)
        expect(result).toBe(5)
    })
    it('randomNumber', () => {
        it('return number less than 2 when number is less than 2')
        const result = randomNumber(0, 10)
        expect(result).toBe(2)

    
    });
});

